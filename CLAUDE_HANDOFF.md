# ChoreoHub 인수인계

## 목표 (2026-08-19, 사용자가 직접 정리)

> 법은 안무를 보호할 수 있지만, 보호에 필요한 기록은 아직 부족합니다.
> **ChoreoHub는 그 기록의 표준을 만들고자 합니다.**

이 한 문장이 판단 기준이다. 기능을 넣을지 말지 고민될 때 **"이게 기록을 더 표준에
가깝게 만드는가?"** 를 물으면 된다.

세 층으로 나누면:

| 층 | ChoreoHub 의 역할 |
| --- | --- |
| 법 | **우리 영역이 아니다.** 판정도 등록도 하지 않는다 |
| **기록의 표준** | **여기가 우리다.** 무엇을·어떤 형식으로·어떻게 검증 가능하게 남기는가 |
| 창작 도구 | 표준을 지키게 만드는 수단 (업로드·오버레이·협업 UI) |

즉 협업 기능도 목적이 아니라 **기록을 남기게 하는 수단**이다. 안무가가 굳이 기록하려고
애쓰지 않아도, 평소 작업하듯 쓰면 표준 기록이 쌓이는 것이 이상적인 형태다.

## 이 프로젝트가 만들고 있는 것

**ChoreoHub**는 “안무가를 위한 GitHub”라는 콘셉트의 모바일/웹 앱이다. 위 목표를 사용자에게
설명하는 비유이며, 목표 자체는 아니다.

핵심 흐름은 다음과 같다.

```
원작 안무 업로드 → 작업/버전 기록 → 공유 범위 설정
→ 허용된 Fork·리믹스·공동작업 → 기여자와 연결된 창작 이력
```

이 앱의 본질은 **안무를 업로드·공유·공동작업하는 플랫폼**이다. AI 유사도 채점이나 법률 판정 앱으로 보이면 안 된다.

- 3D 포즈 데이터는 작품의 구조적 기록과 시각화에 사용한다.
- 라이선스는 창작자가 선택한 공유 범위를 보여 주기 위한 UX다.
- Choreo Passport는 원작·버전·참여자·기여 구간을 이어 보여 주는 창작 이력 카드다.
- 법적 등록, 침해 판정, 자동 정산을 제공한다고 주장하지 않는다.

## 현재 코드와 실행 위치

- 앱 루트: `/Users/moon/Documents/멋사 중앙해커톤/choreohub-mobile`
- Expo 앱: `App.tsx` 한 파일에 현재 화면과 상태가 집중되어 있다.
- MediaPipe 서버: `mediapipe-service/main.py`
- 환경 변수: `.env`

```env
EXPO_PUBLIC_MEDIAPIPE_API_URL=http://localhost:8000
```

### 실행

터미널 1:

```bash
cd "/Users/moon/Documents/멋사 중앙해커톤/choreohub-mobile/mediapipe-service"
.venv/bin/uvicorn main:app --host 127.0.0.1 --port 8000
```

터미널 2:

```bash
cd "/Users/moon/Documents/멋사 중앙해커톤/choreohub-mobile"
npm run web -- --port 8081
```

웹 주소: http://localhost:8081  
서버 상태 확인: http://localhost:8000/health

## 업로드 흐름 (2026-08-18 개편)

예전에는 `POST /v1/motions` 가 분석이 끝날 때까지 응답을 붙잡고 있었다. 그래서 앱은 진행률을
보여 줄 방법이 없었고, 제목·공유 범위는 업로드 전 모달에서만 받았고, 분석 중 게시를 누르면
Alert 로 막혔다. 사용자는 그냥 기다려야 했다.

이제 **영상 저장과 분석이 분리**된다.

```
POST /v1/jobs        영상 저장 → 즉시 {job_id, video_url, width, height, fps, frame_count}
GET  /v1/jobs/{id}   {state, stage, done, total, result?, error?}
POST /v1/motions     (기존 동기 엔드포인트, 호환용으로 유지)
```

`stage` 는 `analyzing`(추론) → `rendering`(미리보기 그리기) → `encoding`(H.264 변환) → `done`.
`JOBS` 는 프로세스 메모리에만 있어 서버 재시작 시 사라진다 — 산출물은 `uploads/` 에 남는다.

## 추론을 4프로세스로 나눈다 (2026-08-18)

측정해 보니 전체 처리 시간의 **80% 가 추론**인데 MediaPipe 는 **코어 하나만** 쓴다
(CPU time 15.5s / wall 15.2s). 해상도를 낮춰도 추론 시간은 그대로다 — MediaPipe 가 내부에서
고정 크기로 리사이즈하기 때문이다. 그래서 유일하게 남는 레버가 프로세스 분할이었다.

```
POSE_WORKERS=4        (환경변수, 기본 4)
POSE_CHUNK_WARMUP=8   (환경변수, 기본 8)
```

구조는 **추론만 병렬, 렌더링은 순차**다. 미리보기 MP4 는 프레임 순서대로 써야 하므로,
`infer_all()` 이 영상을 N등분해 동시에 랜드마크를 뽑고 → `render_previews()` 가 원본을 다시
훑어 순서대로 쓴다. 영상을 두 번 디코딩하지만 디코딩은 전체의 3~7% 라 남는 장사다.

`fork` 가 아니라 **`spawn`** 을 쓴다. 부모(uvicorn)가 MediaPipe/TFLite 를 이미 적재한 상태라
fork 로 복제하면 문제가 생길 수 있다.

### 실측 (6초 / 180프레임 / 1080×1920 / heavy)

| | 전체 | realtime 배수 |
| --- | --- | --- |
| 순차 (이전) | 15.2s | 2.53× |
| 4프로세스 | **9.2s** | 1.53× |

추론만 보면 2.9배 빨라지는데 전체는 1.6배다. 순차로 남은 부분(미리보기 렌더 1.5s +
ffmpeg 1.1s + 두 번째 디코딩 + spawn 4회 기동)이 이제 지배적이기 때문이다.
**앱이 `preview_overlay.mp4` 를 더 이상 쓰지 않으므로**(오버레이를 클라이언트에서 그린다)
이 두 미리보기 생성을 없애면 5~6초까지 줄어든다 — 산출물 다운로드 기능을 포기할 수 있다면.

### 정확도 영향

청크를 나누면 `RunningMode.VIDEO` 의 프레임 간 추적이 경계에서 끊긴다. 순차 결과와 비교한
결과:

- 앱이 사용하는 **x, y 는 평균 1.85px 차이** (1080px 폭 기준). 사실상 동일하다
- 30px 이상 벌어진 프레임은 **180개 중 12개(6.7%)**, 빠른 동작 구간에 몰려 있다
- 차이가 큰 랜드마크는 얼굴 점들(코·눈, 평균 4px)인데 오버레이에서 그리지 않는다
- `z` 는 차이가 크지만(최대 1.23) 단위가 x,y 와 달라 px 환산이 무의미하고, BlazePose 의
  image z 자체가 불안정하다

`POSE_CHUNK_WARMUP` 을 24로 올려도 **효과가 미미했다** (2.10px → 1.87px, 1초 추가). 즉 이
차이는 청크 경계 문제가 아니라 빠른 동작에서 추적이 갈라지는 특성이다. 완전히 동일한 결과가
필요하면 `POSE_WORKERS=1` 로 두면 된다.

### 메모리

워커마다 모델을 따로 적재한다. 4워커 피크 RSS(부모+자식) **824MB**. 메모리가 적은 서버에서는
`POSE_WORKERS` 를 낮춰야 한다.

앱 쪽 결과:

- 업로드가 끝나면 **분석을 기다리지 않고 영상이 바로 뜬다** (`video_url` 을 먼저 받으므로)
- 진행률 카드가 `관절을 읽는 중이에요 · 30/180 프레임` 처럼 단계와 프레임 수를 보여 준다
- **제목과 공유 범위를 분석 중에 입력한다** — 모달에서 New 화면으로 옮겼다
- 분석 중 게시를 누르면 **예약**되고, 끝나는 즉시 자동 게시된다 (다시 누르면 취소)
- 실패하면 카드에 원인과 "다시 시도" 가 뜨고, 영상은 연결된 채로 남아 그냥 게시할 수도 있다

### 페이지를 요소가 아니라 함수 호출로 그린다

`pages[page]()` 로 바꿨다. 페이지 컴포넌트들은 App 리렌더마다 새로 만들어지는 화살표 함수라
`<Current/>` 로 쓰면 타입이 매번 달라져 React 가 트리를 통째로 remount 한다. 진행률 폴링이
600ms 마다 도니 **영상이 계속 처음으로 되감기고 있었다.** 함수 호출은 JSX 를 App 트리에 그대로
펼치므로 `<video>` 노드가 유지된다. 페이지 컴포넌트들이 훅을 쓰지 않아 안전하다 — 앞으로
페이지 안에서 `useState` 등을 쓰려면 그 컴포넌트를 App 밖으로 꺼내야 한다.

## 현재 구현된 화면

1. 홈 / 내 작업
2. 원작 안무 업로드
   - 기존 영상 선택
   - 카메라 촬영
   - 제목·공유 범위 설정 (분석과 동시에 가능)
   - 분석 진행률 카드 · 게시 예약
3. 프로젝트/버전 화면
   - 영상 · 오버레이
   - 포즈 데이터
   - Fork, 공동작업, Choreo Passport 등 콘셉트 화면
4. **안무 영상 화면** (`overlay`) — 원본과 오버레이를 한 화면에서 본다
5. 프레임별 포즈 데이터 화면
   - 33개 관절의 world x/y/z 좌표 및 visibility 표시

### '원본 영상'과 '모션 오버레이'를 합쳤다 (2026-08-18)

두 화면이 사실상 같은 일을 하고 있었다. 오버레이 화면의 **'원본' 모드가 원본 영상 화면과
똑같은 것**을 보여 주고 있어서, 사용자는 같은 영상을 두 경로로 열고 한쪽에서만 모드를
바꿀 수 있었다. 진입 타일도 둘이었다.

- `Media` 화면과 `SourcePlayer` 컴포넌트를 지웠다 (`MotionPlayer` 의 `original` 모드가 대체)
- `Page` 타입에서 `'media'` 를 뺐다
- 버전 화면 타일 2개(`원본 영상`, `모션 오버레이`) → 1개(`영상 · 오버레이`)
- 원본 화면에만 있던 소스 정보(제목·업로드일·원작자·포즈 프레임 수)를 합친 화면으로 옮겼다
- 포즈 데이터가 없는 작업에서는 오버레이/스켈레톤 칩을 흐리게 하고 원본으로 고정한다
- 뒤로가기가 `version` 으로 바로 간다 (예전에는 `media` 를 한 번 거쳤다)

~~현재 신규 업로드 작업물은 앱 메모리에만 저장된다.~~ 2026-08-18 부터 SQLite 에 저장되고 새로고침해도 남는다. 아래 '실제 다중 사용자 협업' 참고.

## MediaPipe 분석 구현 상태

모바일 기기에서 직접 연산하지 않고 **서버에서 MediaPipe Pose Landmarker Heavy**를 돌린다.

- 모델: `pose_landmarker_heavy.task`
- 방식: BlazePose GHUM 3D, 33 landmarks
- 입력: 업로드한 영상
- 처리: 영상의 모든 프레임을 읽어 포즈를 추출
- 제한: 영상 크기 최대 250MB

`POST /v1/motions` 응답에는 다음이 포함된다.

| 결과 | 의미 |
| --- | --- |
| `video_url` | 서버에 저장된 원본 영상 |
| `frames` | 앱에서 프레임별 관절을 표시하기 위한 간결한 데이터 |
| `motion_3d.json` | 전체 프레임 포즈 데이터 |
| `motion_3d.csv` | 프레임 × 관절(33개) 행 데이터 |
| `pose_world_3d.npy` | 3D world 좌표 배열 |
| `pose_image_3d.npy` | 2D 정규화 화면 좌표 배열 |
| `visibility.npy` | 관절별 추적 신뢰도 배열 |
| `preview_overlay.mp4` | 원본 위에 관절 점/뼈대를 입힌 영상 |
| `preview_3d_skeleton.mp4` | 정면·측면·상면 3D 스켈레톤 영상 |

### 관절 오버레이 영상

서버는 원본과 같은 FPS/길이로 `preview_overlay.mp4`를 생성한다.

- 초록색 선: 스켈레톤 연결
- 파란색 점: MediaPipe 관절 랜드마크
- 좌상단: 프레임 번호, 시간, 포즈 인식 상태

**2026-08-18 — 이 MP4는 브라우저에서 재생되지 않고 있었다.** OpenCV `VideoWriter` 가 쓰는
`mp4v`(MPEG-4 Part 2)는 Chrome/Safari 가 디코딩하지 못한다. 실제 확인한 오류:

```
overlay(mp4v): FAIL  DEMUXER_ERROR_NO_SUPPORTED_STREAMS
source(h264):  OK    1080x1920
```

이제 `to_web_h264()` 가 ffmpeg 으로 H.264 재인코딩을 거치며, 응답에
`overlay_web_ready` / `skeleton_web_ready` 로 성공 여부를 알려 준다. ffmpeg 이 없으면
원본을 그대로 두고 `false` 를 돌려준다. `uploads/` 에 남아 있던 기존 산출물도 변환해 두었다.

### 앱의 오버레이는 이제 클라이언트에서 그린다

“모션 오버레이 보기” 화면은 사전 렌더링 MP4 대신 **원본 영상 위에 좌표를 직접 그린다.**

- 모드 전환(원본 / 오버레이 / 스켈레톤만)이 즉각적이다 — 두 번째 영상 파일을 기다리지 않는다
- 재생 시각으로 프레임을 찾아 그리므로 원본과 정확히 맞는다 (`frameAt()` 이진 탐색)
- 포즈가 잡히지 않은 구간이 `frames` 에서 빠져 있어도 시간 기준이라 어긋나지 않는다

**레터박스 보정이 핵심이다.** `VideoView` 는 `contentFit="contain"` 이라 1080×1920 영상이
310px 스테이지 안에서 좌우에 여백을 두고 그려진다. 예전 코드는 관절 점을 스테이지 전체에
매핑해서 실제로는 몸에서 크게 밀려 있었다. `PoseOverlay` 는 영상이 실제로 그려지는 상자를
계산해 그 안에 찍는다. 측정값(스테이지 436×310, 영상 상자 174×310, 좌우 여백 131px)에서
모든 뼈대가 상자 안에 들어오는 것을 확인했다.

재생 위치는 `playbackRef` 에 보관한다. 페이지 컴포넌트가 App 리렌더마다 새로 만들어져
`MotionPlayer` 가 remount 되는 구조라, 이게 없으면 모드를 바꿀 때마다 영상이 처음으로
되감긴다.

실제 테스트 결과:

- 업로드 → 분석 → 게시 → 오버레이까지 브라우저에서 전 과정 확인 (180 프레임 클립)
- 뼈대 26개 선 + 관절 22개 점 렌더링, 전부 영상 상자 내부
- 모드를 오가도 재생이 이어짐, 콘솔 오류 없음
- `npx tsc --noEmit` 통과

## 참고로 제공된 예시 결과

사용자가 제공한 참조 폴더:

```
/Users/moon/Desktop/안무_3d/
```

포함된 형식:

- `안무.mp4`
- `motion_3d.json`, `motion_3d.csv`
- `pose_world_3d.npy`, `pose_image_3d.npy`, `visibility.npy`
- `preview_overlay.mp4`
- `preview_3d_skeleton.mp4`

서버는 이 형식과 동일한 목적의 산출물을 생성하도록 맞춰져 있다. 정확한 바이트 단위 동일성이 아니라, 파일 구조와 시각 결과의 일관성을 목표로 했다.

## 현재 서버에 남아 있는 테스트 산출물

`mediapipe-service/uploads/`에 실제 업로드 영상과 산출물이 있다. 파일명은 원본 SHA-256 해시 기반이다.

- `3253044...mp4`: 업로드 원본
- `3253044...preview_overlay.mp4`: 오버레이 결과
- `3253044...preview_3d_skeleton.mp4`: 3D 3면 뷰 결과
- 같은 접두사의 JSON/CSV/NPY 데이터

이 폴더는 로컬 개발용이다. 운영 서비스에서는 객체 스토리지와 DB 메타데이터로 분리해야 한다.

## 중요한 UX·문구 원칙

사용자가 다음 방향을 명확히 요청했다.

- 앱 안에서 법적 판단이나 권리 주장 문구를 전면에 내세우지 않는다.
- “기본 증빙”, “권리 변동” 같은 표현은 피한다.
- 대신 “작업 기록”, “함께 만든 사람”, “공유 설정”, “버전 히스토리”처럼 창작자 친화적 언어를 쓴다.
- 서비스는 훗날 안무 창작 관행과 기준을 논의할 때 참고될 수 있는 구조적 기록을 제공할 수 있으나, 현재 앱은 이를 법률 서비스로 표현하지 않는다.

## 우선순위가 높은 다음 작업

1. **앱에서 실제 산출물 더 잘 보여 주기**
   - ~~오버레이 영상의 현재 재생 시점과 관절 데이터 프레임 동기화~~ → 2026-08-18 완료
   - 3D 스켈레톤 MP4 재생 화면 연결 (`previewSkeletonUri` 는 저장만 되고 아직 화면이 없다)
   - JSON/CSV/NPY 산출물 목록/다운로드 UI
   - world 좌표로 3D 뷰를 앱 안에서 직접 그리기 (오버레이와 같은 방식으로 가능하다)

2. **데이터 영속화** → 2026-08-18 완료 (SQLite)
   - 남은 것: 실제 인증(현재 user_id 만 알면 사칭 가능), 버전/Fork 이력 테이블,
     `uploads/` 를 객체 스토리지로 분리, 잡 상태도 DB 로

3. **업로드 안정성**
   - ~~서버 처리 진행률, 실패 사유, 재시도 UX~~ → 2026-08-18 완료
   - ~~긴 영상 작업을 백그라운드 잡으로 분리~~ → 2026-08-18 완료 (`POST /v1/jobs`)
   - ~~생성된 MP4의 웹 호환 인코딩(H.264) 보장~~ → 2026-08-18 완료 (`to_web_h264`)
   - 남은 것: 잡을 프로세스 메모리가 아니라 디스크/큐에 두기 (서버 재시작 시 진행 중 작업 소실),
     업로드 자체의 진행률(현재는 서버 분석 진행률만 표시), 동시 업로드 여러 건 처리

4. **제품 핵심 기능 강화**
   - ~~공동작업자 초대 및 기여 구간 표시~~ → 2026-08-18 완료 (아래 참고)
   - 원작을 기준으로 한 Fork 생성
   - 버전 간 변경 구간 메모
   - 라이선스/공유 범위에 맞는 리믹스 업로드 흐름
   - 수정 제안(pull request) 실제 흐름 — 현재 협업 화면은 참여자 관리까지만 있다

## 실제 다중 사용자 협업 (2026-08-18)

상태가 앱 메모리에 있는 한 "협업"은 흉내일 수밖에 없었다. 이제 **SQLite(`choreohub.db`)에
서버가 원본을 들고 있고**, 두 사람이 각자 브라우저에서 같은 작업을 본다.

### 신원

비밀번호가 없다. 처음 열면 표시 이름을 받아 `users` 행을 만들고 `user_id` 를 브라우저
`localStorage('choreohub.me')` 에 저장한다. 새로고침해도 같은 사람으로 돌아온다.

**`user_id` 를 아는 사람은 그 사람으로 행동할 수 있다.** 데모용이며 공개 배포 전에 실제
인증으로 바꿔야 한다. 네이티브에는 저장소 패키지가 없어 세션 동안만 유지된다
(`@react-native-async-storage/async-storage` 를 넣으면 웹과 같아진다).

### API

```
POST   /v1/users                                     표시 이름 → {user_id, name}
GET    /v1/projects?user_id=                         내가 만든 + 초대받아 참여한 작업
POST   /v1/projects                                  게시 (motion 참조를 함께 넘긴다)
GET    /v1/projects/{id}/frames                      오버레이용 랜드마크 (필요할 때만)
POST   /v1/projects/{id}/collaborators               초대
PATCH  /v1/projects/{id}/collaborators/{cid}         수정
DELETE /v1/projects/{id}/collaborators/{cid}         해제
GET    /v1/invites/{code}                            참여 전 미리보기
POST   /v1/invites/{code}/join                       초대 코드로 참여
```

권한은 서버가 강제한다 — 소유자이거나 `직접 수정` 인 사람만 초대·수정할 수 있고, 그 외에는
403 이다. 모르는 `user_id` 는 401.

포즈 좌표는 DB 에 넣지 않는다. 60초 영상이 8MB 라 SQLite 에 넣을 이유가 없고 이미
`uploads/<해시>.*` 에 있으므로, 프로젝트 행은 `source_sha256` 으로 가리키고 앱은
`/v1/projects/{id}/frames` 로 필요할 때만 받아 간다. 이를 위해 `analyze()` 가
`<해시>.app_frames.json` 을 따로 남긴다.

### 갱신

4초 폴링이다 (`library`/`version`/`collab`/`home` 화면에서만). WebSocket 은 데모 규모에
불필요하다고 판단했다.

### 검증 (브라우저 컨텍스트 2개 = 실제 사용자 2명)

| 시나리오 | 결과 |
| --- | --- |
| A·B 각자 로그인, 데이터 분리 | 통과 |
| B 는 빈 목록으로 시작 | 통과 |
| B 가 초대 코드로 참여 → A 의 작업이 보임 (원작자 표시 포함) | 통과 |
| A 화면이 폴링으로 B 를 자동 표시 | 통과 |
| A 가 B 를 '직접 수정' 으로 승격 → B 화면에 반영 | 통과 |
| 승격된 B 가 MIA 초대 → A 도 폴링으로 확인 | 통과 |
| 새로고침 후 로그인·참여 유지 | 통과 |

### 다른 기기에서 접속하려면

서버는 이제 `--host 0.0.0.0` 으로 뜬다. 앱의 `.env` 를 컴퓨터의 LAN IP 로 바꿔야 한다.

```
EXPO_PUBLIC_MEDIAPIPE_API_URL=http://<컴퓨터 LAN IP>:8000
```

`localhost` 로 두면 휴대폰에서는 휴대폰 자신을 가리킨다.

## 구간을 카운트에서 시간으로 (2026-08-20)

구간이 **사용자가 타이핑한 카운트 정수**였다. 검증할 방법이 없었다 — `bpm`·`tempo` 로직이
코드에 전혀 없어서 `count 9` 가 영상의 몇 초인지 시스템이 몰랐다. 3초 클립에 `count 1–64` 를
넣어도 통과했고, 그래서 안무 영상 화면의 「기록된 구간」이 더미로 남아 있었다.

이제 **작품 타임라인 위의 밀리초**다.

```
versions.start_ms / end_ms / duration_ms   (기존 count_from/to 는 미사용으로 남김)
projects 응답에 workMs 추가
```

### 왜 '현재 영상의 초' 가 아니라 '작품 타임라인의 초' 인가

버전마다 영상이 바뀌는 것이 이 앱의 핵심 흐름이다. 구간을 현재 영상 기준으로 저장하면 영상이
교체될 때 구간이 의미를 잃는다. 그래서 **v1 의 영상 길이가 작품 타임라인을 정의**하고
(`work_ms()`), 이후 버전은 그 타임라인 위의 구간을 주장하며 자기 클립을 근거로 붙인다.

### 얻은 것

- **검증이 된다.** 작품 길이를 넘는 구간은 서버가 400 으로 거절한다 (`작품 길이(0:06)를 넘는 구간입니다`)
- **포즈 데이터와 직접 연결된다.** 구간이 `frames[].time_ms` 와 같은 축이라 그 구간의 프레임·관절을 바로 집을 수 있다
- **입력이 가벼워졌다.** BPM 도, "1카운트=N초" 도 받지 않는다. `0:08` 또는 `8` 을 적으면 된다
- **더미가 사라졌다.** 「기록된 구간」이 실제 버전 구간 목록이 됐고, 줄을 누르면 그 지점으로 재생 위치가 옮겨진다

### 잃은 것 (알고 선택했다)

시간은 녹화물에 붙고 카운트는 작품에 붙는다. 같은 안무를 **다른 템포로 다시 촬영**하면 초는
밀리는데 카운트는 그대로다. 작품 타임라인으로 저장해 버전 간 영상 교체는 견디지만, 작품
전체를 재촬영하는 경우는 여전히 어긋난다.

필요해지면 원작에 "1카운트 = N초" 하나만 받아 **카운트 라벨을 파생**해 함께 보여줄 수 있다.
저장은 시간, 표시는 둘 다.

### 화면 반영

- 제안 시트: 「고친 구간 · 분:초」 두 칸. `0:08` 도 `8` 도 받는다. 작품 길이를 안내한다
- 구간별 크레딧 맵: 축이 카운트 → **작품 타임라인**. 1초 버킷으로 소유자를 채운다
- 지분: `16 카운트` → `6초` / `1분 6초`
- 버전 라벨: `main · v2 · 0:02–0:04`

### 검증

| 항목 | 결과 |
| --- | --- |
| 잘못된 형식 (`2:99`) | `시간 형식을 확인해 주세요` |
| 끝 < 시작 | 안내 표시 · 서버도 400 |
| 작품 길이 초과 | 안내 표시 · 서버도 400 |
| 초만 입력 (`2`) | `0:02` 로 정상 등록 |
| 「기록된 구간」 | 실제 버전 구간, 옛 더미 사라짐 |
| 크레딧 맵 | `작품 타임라인 · 전체 0:06`, '카운트' 문자열 잔존 없음 |

### 기존 데이터

마이그레이션은 `init()` 에서 `ALTER TABLE ADD COLUMN` 으로 처리한다. **카운트로 저장된 옛
행은 `start_ms` 가 NULL 이라 '전체' 로 표시된다.** 카운트↔시간 매핑이 없으므로 변환하지
않았다 — 없는 값을 만들어 넣으면 기록의 표준이라는 목적에 어긋난다. 전부 검증용 테스트
데이터이므로 필요하면 DB 를 초기화하면 된다.

## 다크 테마 전환 (2026-08-20)

앱 전체를 어두운 팔레트로 바꿨다. 색이 91종 374회 하드코딩되어 있어서, 색값을 하나씩
바꾸는 대신 **속성별 역할 매핑**으로 일괄 치환했다.

`#FFF` 하나가 카드 배경(37회)과 색 버튼 위 글씨(17회) 두 역할로 쓰이고 있었다. 그래서
`(속성, 원래색) → 새 색` 표를 만들어 `backgroundColor: '#FFF'` 는 카드 표면으로,
`color: '#FFF'` 는 흰색 그대로 남겼다. 변환 스크립트는 표에 없는 조합이 하나라도 나오면
멈추게 해서 빠뜨림이 없는지 확인했다.

### 팔레트

색은 여전히 StyleSheet 안의 리터럴이다. 새 화면을 만들 때 아래 값을 쓰면 톤이 맞는다.

| 역할 | 값 |
| --- | --- |
| 앱 바탕 | `#0E1317` |
| 카드 · 시트 | `#181F26` |
| 입력 · 트랙 | `#101720` / `#131A20` |
| 눌린 표면 (태그·격자) | `#242E36` |
| 더 깊은 카드 (초대·패스포트·코드뷰) | `#0A0F14` |
| 경계 | `#2A333B` (옅은 것 `#222A31`) |
| 본문 글씨 | `#E8EDF2` |
| 보조 글씨 | `#A2AFB9` |
| 흐린 글씨 | `#77848D` |
| 파랑 (`PURPLE`) | `#7FA5FF` · 배경 `#1B2740` · 경계 `#33456B` |
| 초록 | `#4FC7A2` · 배경 `#12291F` |
| 앰버 | `#E0AE3C` · 배경 `#2A2314` |
| 빨강 | 글씨 `#FF8B77` · **채움 `#E4573E`** |

빨강은 글씨용과 채움용을 나눴다 — 밝은 살몬을 FAB 배경에 쓰면 어두운 바탕에서 힘이 없다.

기여 그래프의 빈 칸(`CONTRIB_SCALE[0]`)은 카드보다 **밝아야** 격자가 읽힌다 (`#242E36`).
어두운 테마에서 "빈 칸 = 더 어둡게" 로 두면 배경에 묻힌다.

`app.json` 에 `userInterfaceStyle: "dark"` 와 `backgroundColor: "#0E1317"` 을 넣고
`StatusBar` 를 `light` 로 바꿨다.

### 다크 전환 후 남았던 대비 문제 (같은 날 수정)

색을 역할별로 바꿨어도 **"밝은 채움 위의 밝은 글씨"** 조합이 남았다. 눈으로 찾는 대신
모든 화면의 텍스트에 대해 실제 명도 대비를 계산하는 스크립트를 돌렸다 (WCAG AA: 일반
4.5:1, 큰 글씨 3:1). 처음 **10개 조합**이 걸렸고 전부 고쳐 **0개**가 됐다.

| 문제 | 원인 | 조치 |
| --- | --- | --- |
| `NOW IN YOUR ARCHIVE` 안 보임 (대비 1.0) | 카드 배경과 라벨 색이 **둘 다** `#7FA5FF` 가 됐다 | 홈 대표 카드를 밝은 파랑 채움 → **어두운 파랑 패널**(`#1B2740`)로 |
| `Fork 관계 연결하기` 카드 (1.7) | 앰버 채움 위에 밝은 회색 글씨 | 앰버 틴트 패널(`#2A2314`)로, 아이콘만 앰버 채움 |
| `전환부 발 방향` 등 검정 글씨 (1.12) | `<Text style={s.grow}>` — **색이 지정되지 않아** 기본 검정 | `declinedTitle` · `activityText` 색 추가 |
| 흰 글씨 버튼 36곳 (2.41) | `PURPLE` 이 밝은 파랑이 되어 **흰 글씨가 안 읽힌다** | 밝은 채움 위 글씨를 어두운 `#0B1116` 로 (primary·merge·join·follow·모드칩·다운로드) |
| `＋` FAB (1.63) | 공용 `navIcon` 회색이 붉은 채움 위에 | FAB 전용 `navPlusIcon` 흰색 + 굵게, 채움도 진하게 |
| 흐린 글씨 73곳 (4.33) | `#77848D` 가 카드 위에서 기준 미달 | `#8B99A3` 로 한 단계 밝게 |

**교훈**: 다크 전환에서 위험한 것은 배경이 아니라 **"원래 밝은 색이던 채움"** 이다. 파랑·앰버
채움은 어두운 테마에서 *가장 밝은 면*이 되므로, 그 위의 글씨는 방향을 뒤집어야 한다.

대비 측정 스크립트는 회귀 검사에 다시 쓸 수 있다 — 새 화면을 추가한 뒤 한 번 돌리면 된다.

### 확인

홈 · 내 작업 · 버전 · 작업 기록 · 안무 영상 · 기록 파일 · 프로필 · 탐색 여덟 화면을 캡처해
확인했다. 8000px² 이상 요소 중 밝기 150 이상인 것은 없다(밝은 면이 남지 않았다는 뜻).
콘솔 오류 없음, `npx tsc --noEmit` 통과.

**단일 테마다.** 라이트/다크 토글이 필요하면 이 리터럴들을 토큰 객체로 한 번 더 빼야 한다.

## 구간별 크레딧과 반영 흐름 (2026-08-20)

작업 기록 화면에 트리 위로 두 가지를 더 얹었다. 크레딧 트리는 "무엇이 남았나"를 보여 주고,
이 둘은 **"이 작품의 어느 부분이 누구 것인가"** 를 보여 준다.

### 구간별 크레딧 맵

**계산 규칙 (2026-08-20 보강)**

```
v1(원작)  → 전 구간을 바탕으로 깐다
이후 버전 → 자기 구간만 덮어쓴다  ("가장 나중에 고친 사람이 그 구간의 크레딧")
구간 없는 후속 버전 → 맵에서 제외
```

구간 없는 버전이 전체를 주장하면 **남의 크레딧을 전부 삼켜 버린다.** 실제로 카운트→시간
전환 뒤 옛 행의 `start_ms` 가 NULL 이 되어 마지막 버전 작성자가 100% 를 가져가는 상태가
있었다. 두 가지로 막았다.

1. **제안 시 구간 필수** — 서버가 400 (`고친 구간을 지정해 주세요…`), 앱도 제출 전에 막는다.
   원작 v1 만 예외이며 `create_project` 안에서 직접 만들어진다
2. **맵에서 제외** — 구간 없는 후속 버전은 계산에 넣지 않고, 몇 개를 제외했는지 화면에 밝힌다

작품 길이를 모르는 옛 작업은 `workMs → max(durationMs) → max(endMs)` 순으로 대체하고,
길이 정보가 없다는 사실을 축 설명에 표시한다.

**이 맵이 의미하지 않는 것** (오해를 부르기 쉬운 지점)

- 최초 창작자 표시가 아니다 — 남이 내 구간을 고치면 그 구간 크레딧은 넘어간다
- 창작 기여도가 아니다 — 시간 길이로만 센다
- 정산 근거가 아니다

**검증** (40초 작품, 두 기여자)

```
v1 전체        윤가희
v2 0:08–0:16   차민준
v3 0:32–0:40   차민준
v4 0:20–0:24   윤가희

막대: 0:00–0:08 파랑(78px) │ 0:08–0:16 초록(78px) │ 0:16–0:32 파랑(152px) │ 0:32–0:40 초록(78px)
지분: 윤가희 24초 60%  ·  차민준 16초 40%      ← 예상과 정확히 일치
```

가로 막대를 8카운트 축으로 나눠 각 구간을 소유자 색으로 칠한다.

```
│ 1–8  강다현 │ 9–16  오세훈 │ 17–24  임하늘 │ 25–32  오세훈 │
  오세훈 16 카운트 50%   강다현 8 카운트 25%   임하늘 8 카운트 25%
```

계산 규칙: **v1(전체)이 바탕이고 번호가 큰 버전이 자기 구간을 덮어쓴다.** 즉 "가장 최근에
그 구간을 고친 사람"이 그 구간의 크레딧을 갖는다. 구간을 `count_from`/`count_to` 정수로
저장해 둔 덕에 이 계산이 가능하다 — 자유 텍스트였다면 못 한다.

원작자는 항상 첫 색(브랜드 파랑)을 받고, 나머지는 구간 순서로 배정된다. 화면을 다시 열어도
같은 색이 나온다.

### 반영 흐름도

`v1 ──▶ v2 ──▶ v3 ──▶ v4` 를 가로로 스크롤하며 보여 준다. 각 노드는 버전 번호·구간·작성자·
영상 첨부 여부를 담고, **화살표 라벨은 그 버전을 main 에 반영한 사람**이다 (본인이 반영했으면
'본인 반영'). 노드 테두리 색은 구간 맵과 같은 기여자 색이라 두 그림이 서로 읽힌다.

검증 (v1 전체/임하늘 → v2 09–16/오세훈 → v3 25–32/오세훈 → v4 01–08/강다현):

| 항목 | 결과 |
| --- | --- |
| 구간 블록 4개, 기여자별 색 구분 | 통과 |
| 지분 계산 | 오세훈 50% · 강다현 25% · 임하늘 25% (32카운트 기준 정확) |
| 흐름도 노드 4개 · 화살표 3개 | 통과 |
| 화살표 라벨에 반영자 표시 | `임하늘 반영` |

## 작업 삭제 (2026-08-20)

```
DELETE /v1/projects/{id}?user_id=
```

**원작자만** 지울 수 있다. `직접 수정` 권한으로는 안 된다 (403) — 작품 전체를 없애는 것은
메타데이터 수정과 무게가 다르다. 앱에서도 위험 구역이 원작자에게만 보인다.

- 버전·참여자는 `ON DELETE CASCADE` 로 함께 사라진다 (실제 동작 확인)
- **업로드 파일은 지우지 않는다.** 파일 이름이 내용 해시라 같은 영상을 올린 다른 작업이 같은
  파일을 가리킬 수 있다. 지우면 남의 작업이 깨진다. 파일 정리는 참조 카운트를 세는 별도
  작업으로 다뤄야 한다 (미구현)

### 확인 절차

RNW 의 `Alert` 는 no-op 이라 확인 대화상자를 쓸 수 없다. GitHub 저장소 삭제처럼
**작업 이름을 그대로 입력**해야 버튼이 동작한다. 위험 구역에 무엇이 사라지는지도 숫자로 밝힌다:
`버전 2개와 참여자 1명의 기록이 함께 사라집니다. 되돌릴 수 없습니다.`

### 검증

| 시나리오 | 결과 |
| --- | --- |
| `수정 제안` 권한자 | 수정 행 자체가 안 보임 |
| `직접 수정` 권한자 | 수정 행 보임 · **위험 구역 없음** · 서버도 403 |
| 원작자 | 수정 행·위험 구역 모두 보임 |
| 낯선 `user_id` | 401 |
| 이름 불일치 | `이름이 일치하지 않아요` · 작업 그대로 남음 |
| 이름 일치 | 삭제 → 목록으로 이동, 목록에서 사라짐, 서버 404 |
| 이미 삭제된 것 재삭제 | 404 |

## 게시 후 작업 정보 수정 (2026-08-20)

원작자가 게시한 뒤에는 제목도 공유 범위도 바꿀 수 없었다. 서버에 `PATCH /v1/projects/{id}` 는
처음부터 있었지만 **앱에서 호출하는 곳이 없었다.**

버전 화면 hero 아래에 **작업 정보 수정** 행을 두고 시트에서 이름·공유 범위를 고친다.
`canEdit`(원작자 또는 `직접 수정`)일 때만 보이고, 서버도 `require_editor` 로 같은 기준을 강제한다.

**영상은 여기서 바꾸지 않는다.** 시트에 "영상을 바꾸려면 main 에 버전 올리기로" 라고 안내한다 —
영상 교체는 무엇이 언제 바뀌었는지 남아야 하므로 버전 이력을 거쳐야 한다. 제목·공유 범위는
작품의 메타데이터라 이력 없이 고쳐도 기록이 흐려지지 않는다.

`_sync_project_head()` 가 관리하는 `version` 라벨은 건드리지 않는다 (확인함).

검증:

| 시나리오 | 결과 |
| --- | --- |
| 원작자 · `직접 수정` 권한자에게 수정 행 보임 | 통과 |
| `수정 제안` 권한자에게는 안 보임 | 통과 (서버도 403) |
| 낯선 `user_id` | 403 |
| 빈 이름 저장 | `제목이 필요해요` 안내 |
| 이름·공유 범위 저장 | 화면·목록·탐색 피드에 즉시 반영 |
| `연습 전용` 으로 바꾸면 탐색 피드에서 제외 | 통과 |
| 버전 라벨 `main · v2 · count 09–16` 유지 | 통과 |

떠 있는 토스트는 헤더를 가리지 않게 하단(`bottom: 92`)으로 옮겼다. 시트 안에서는
`SheetToast()` 가 따로 뜨므로 위로 올릴 필요가 없다.

## 영상이 바뀌어도 예전 스켈레톤이 남던 문제 (2026-08-19)

사용자가 두 가지로 신고했는데 원인은 하나였다.

> "영상의 길이가 안 맞으면 수정이 안 되는 것 같다"
> "main 에 업로드했을 때 스켈레톤이 이전 것으로 표시된다"

프레임 캐시가 **프로젝트 id 로만** 묶여 있었다.

```tsx
// 이전 — 영상이 바뀌어도 다시 받지 않는다
if (!selected?.sourceSha256 || projectFrames.id === selected.id) return;
```

main 에 새 버전이 반영되면 작업의 `source_sha256` 이 바뀌는데 id 는 그대로다. 그래서 영상만
새것으로 갈리고 관절은 예전 것을 계속 그렸다. 길이가 다르면 특히 크게 어긋나 "수정이 안 된
것처럼" 보였다.

캐시 키에 영상 해시를 넣고(`${id}:${sourceSha256}`), 영상이 바뀌면 **재생 위치도 되돌린다** —
6초 클립의 5초 지점을 기억한 채 1초 클립으로 갈리면 그것만으로도 어긋난다.

서버는 문제가 아니었다. 길이·fps·해상도를 바꾼 클립 4종(0.4초/1초/12fps/642×1140)을 모두
정상 처리하는 것을 확인했다.

검증: 6초 180프레임 원작 → 3초 36프레임 클립을 반영했을 때

| 항목 | 결과 |
| --- | --- |
| 영상 길이 | 6.0s → 3.0s 교체 |
| 포즈 프레임 표기 | 180 → **36** 으로 갱신 |
| 스켈레톤 | 새 데이터로 26개 뼈대 렌더 |
| 재생 위치 | 4.92s → 리셋 |

## 크레딧 미리보기 (트리) (2026-08-19)

작업 기록 화면에 **크레딧 트리**를 넣었다. 버전 사슬·참여자·검토 상태·공유 설정을 하나의
트리로 묶어, 이 작업이 최종적으로 어떤 크레딧이 되는지 미리 보여 준다.

```
크레딧 미리보기
├─ 원작
│  └─ 임하늘            2026-08-19 · 전체 · 포즈 180f
├─ main · 2개 버전
│  ├─ v1 전체           원작 등록 · 임하늘 · 영상 180f
│  └─ v2 count 09–16    후렴 팔 라인 변형 · 오세훈 · 임하늘 반영 · 영상 32f
├─ 함께 만든 사람 · 2명
│  ├─ 오세훈            팔 라인 · count 09–16 · 수정 제안 · 참여 중
│  └─ 강다현            포메이션 · count 25–32 · 직접 수정 · 참여 중
├─ 검토 중 · 1건
│  └─ count 25–32 엔딩 포즈 제안
├─ 반영하지 않음 · 1건
│  └─ count 17–24 전환부 발 방향
└─ 공유 설정
   ├─ 리믹스 허용
   └─ 기록 형식 · choreohub-motion-3d/v1
```

`flattenTree()` 가 `CreditNode[]` 를 연결선이 붙은 평면 행으로 펼친다. 각 행은 `prefix`(그 줄의
가지)와 `cont`(부제 줄에 세로선을 이어 주는 문자열)를 함께 갖는다 — `cont` 가 없으면 설명 줄에서
트리가 끊겨 보인다. 연결선은 고정폭 글자라 들여쓰기가 정확히 맞는다.

## Alert 이 웹에서 아무 일도 하지 않았다 (2026-08-19)

사용자가 "제안 보내기를 눌렀는데 작동이 안 된다"고 알려 줘서 찾았다. 원인은 이 화면이 아니라
**react-native-web 의 `Alert` 구현**이었다.

```js
// node_modules/react-native-web/dist/exports/Alert/index.js
class Alert { static alert() {} }
```

완전한 no-op 이다. 앱에 `Alert.alert` 가 18곳 있었고 **웹에서 전부 침묵**했다. 그래서
제목을 비우거나 구간을 한쪽만 채우면 검증은 정상적으로 막는데 안내가 보이지 않아
"버튼이 안 눌린다"로 보였다. 게시 실패·저장 실패·초대 실패 안내도 모두 사라지고 있었다.

`notify(title, body, kind)` 토스트로 18곳 전부 바꿨다. `Alert` import 는 제거했다.

**RNW `Modal` 은 `document.body` 에 포털로 붙는다** (`ModalPortal.js` → `appendChild`).
그래서 떠 있는 토스트에 `zIndex: 10000` 을 줘도 시트에 가려진다 — 실제로 `elementFromPoint`
로 확인했다. 시트 안에도 같은 카드를 렌더링하는 `SheetToast()` 를 두고, 시트가 열려 있으면
떠 있는 쪽은 숨긴다.

검증 (예전에는 전부 침묵했던 경로들):

| 상황 | 결과 |
| --- | --- |
| 이름 없이 시작하기 | `이름이 필요해요` 표시 |
| 영상 없이 게시 | `영상이 필요해요` 표시 |
| 제목 없이 제안 보내기 | `제목이 필요해요` 표시 (시트 안, 실제로 보임) |
| 구간을 한쪽만 채움 | `구간을 확인해 주세요` 표시 |
| 끝 < 시작 | 범위 안내 표시 |
| 정상 입력 | 성공 토스트 + v2 생성 + 시트 닫힘 |

**교훈**: 성공 경로만 테스트해서 놓쳤다. 앞으로 검증 실패 경로도 눌러 볼 것.

## 작품 길이는 늘어날 수 있다 (2026-08-20)

예전에는 작품 길이를 넘는 구간을 400 으로 거절했다. 그런데 **뒷부분을 다시 짜면서 안무가
길어지는 것**은 정상적인 창작이다. 이제 확장을 허용한다.

```python
work_ms() = max(v1 영상 길이, 반영된 버전들의 end_ms)
```

- 제안 상태는 포함하지 않는다 — 반영돼야 작품이 길어진다
- 오타(`90:00`)만 막는다: 현재 길이 + 10분을 넘으면 400
- 제안 시트에서 끝 시간이 작품보다 뒤면 실시간으로 알린다:
  `작품이 1:10 → 1:30 로 길어집니다`
- 반영 후 결과 안내에도 표시한다: `작품이 1:10 → 1:30 로 길어졌습니다`

### 겹치지 않는 구간은 여러 사람이 각자 반영된다

질문받은 시나리오를 그대로 돌려 확인했다 — 60초 원본에 B가 0:10–0:20, C가 0:40–1:10.

```
v1 전체        안무가A
v2 0:10–0:20   안무가B     ← 원작자가 반영
v3 0:40–1:10   안무가C     ← 원작자가 반영, 작품이 60초 → 70초로

막대: 0:00–0:10 A(57px) │ 0:10–0:20 B(57px) │ 0:20–0:40 A(110px) │ 0:40–1:10 C(163px)
지분: 안무가A 30초 43%  ·  안무가C 30초 43%  ·  안무가B 10초 14%
```

구간이 겹치지 않으면 **충돌 없이 둘 다 반영된다.** 겹치면 나중에 반영된 쪽이 그 구간의
크레딧을 갖는다 (덮어쓰기). 겹침 경고는 아직 없다.

### 구간 영상만 따로 보기 (2026-08-20)

버전에 첨부된 클립을 그 버전 기준으로 재생한다.

```
GET /v1/projects/{id}/versions/{vid}/frames   그 버전 클립의 랜드마크
```

- 버전 카드의 **「구간 영상 보기」**, 안무 영상 화면의 **「기록된 구간」** 줄에서 들어간다
- 클립 모드에서는 제목이 `v2 구간 영상 · 0:02–0:05` 로 바뀌고, 배너에 작성자·구간·포즈 프레임
  수가 뜬다. 「작품 전체 보기」 로 돌아온다
- 오버레이·스켈레톤 모드가 그대로 동작한다 (그 클립의 관절 데이터로)
- 영상이 없는 버전(메모만)은 안내를 띄우고 열지 않는다

### 정본 영상은 전체를 다루는 버전만 교체한다

이 기능을 붙이며 규칙을 바로잡았다. 예전에는 **어떤 버전이 반영되든** 작품의 정본 영상이 그
버전의 클립으로 교체됐다. 그래서 `0:02–0:05` 구간을 고친 3초 클립이 **6초 작품 전체를 대체**해
버렸고, "작품 전체 보기" 와 "구간 영상 보기" 가 같은 파일을 재생했다.

이제 head 버전이 **전체를 다룰 때만** 정본을 교체한다.

```python
covers_all = head.startMs is None or (head.startMs == 0 and head.endMs >= work_ms)
```

- 전체 재촬영 → 정본 교체 (의도한 동작 유지)
- 구간 수정 → 정본은 그대로, 클립은 그 버전에 붙어 「구간 영상 보기」로 재생

검증 (6초 원본 + `0:02–0:05` 구간에 3초 클립):

| 화면 | 영상 | 포즈 프레임 |
| --- | --- | --- |
| 작품 전체 | `84763a7c…` 6.0s | 180 frames |
| v2 구간 영상 | `da795ce1…` 3.0s | 36 frames |
| 복귀 후 | `84763a7c…` 6.0s | 180 frames |

### 남은 한계 — 영상은 합성되지 않는다

작품 타임라인은 70초인데 **재생되는 영상은 마지막으로 반영된 버전의 클립 하나**다.
위 시나리오에서 확인한 상태:

```
작품 타임라인 90초
현재 재생 영상  /uploads/c.mp4 (30초)
  v1 전체        60초 영상
  v2 0:10–0:20   10초 영상
  v3 0:40–1:10   30초 영상   ← 이게 현재 영상이 된다
```

위 두 변경으로 **1번(정본 고정 + 구간별 클립 재생)을 택한 상태**다. 남은 것은 2번 —
ffmpeg concat 으로 구간을 이어 붙인 정본을 생성하는 것. 그러면 작품 타임라인과 재생 영상이
완전히 일치한다. 길이·해상도·fps 정규화가 필요해 처리 시간이 늘어난다.

지금은 타임라인이 70초여도 정본 영상은 원본 60초다. 확장된 뒤쪽 10초는 그 버전의 구간
영상으로만 볼 수 있다.

## 현재 버전(HEAD) 지정 (2026-08-20)

여러 버전이 쌓였을 때 어느 것을 현재 상태로 볼지 고를 수 있다. **이력은 지우지 않고
가리키는 곳만 바꾸는 포인터 방식**이다 — 되돌린다는 사실 자체가 기록의 일부다.

```sql
projects.head_version_id   -- 비어 있으면 가장 최근 반영본
projects.head_set_by / head_set_at
```

```
POST /v1/projects/{id}/versions/{vid}/head   {user_id}
```

- 반영된 버전만 지정할 수 있다 (제안·거절 상태는 409)
- 권한은 `require_editor` — 원작자와 `직접 수정` 만
- `main_head()` 가 포인터를 먼저 보고, 없으면 `MAX(number)` 로 떨어진다
- **새 버전이 반영되면 지정이 자동으로 풀린다** (`clear_head_pin`). 이게 없으면 되돌린 상태에서
  반영해도 화면이 그대로여서 아무 일도 없어 보인다

### 무엇이 함께 따라 움직이나

| 대상 | 동작 |
| --- | --- |
| 작업의 현재 영상 · 포즈 데이터 | HEAD 버전 것으로 교체 (`_sync_project_head`) |
| 작업 라벨 | `main · v2 · 0:08–0:16 · **되돌림**` (뒤에 더 최신 버전이 있을 때) |
| 구간별 크레딧 | **HEAD 보다 뒤의 버전은 제외** — 현재 상태가 아니므로 |
| 앱의 프레임 캐시 | 무효화 + 재생 위치 리셋 |

### 화면

main 그래프의 각 버전 카드에 「이 버전을 현재로」 버튼이 붙는다 (현재 버전에는 없음).
HEAD 는 `현재` 배지와 파란 점으로 표시한다. 직접 지정한 상태면 목록 위에 누가 언제 지정했는지
와 "이후 버전은 이력에 남아 있지만 현재 상태에는 반영되지 않습니다" 를 알린다.

### 검증

```
v1 전체/윤가희 · v2 0:08–0:16/차민준 · v3 0:32–0:40/차민준 · v4 0:20–0:24/윤가희  (40초 작품)

HEAD=v4  → 윤가희 24초 60%  ·  차민준 16초 40%
HEAD=v2  → 윤가희 32초 80%  ·  차민준  8초 20%      ← v3·v4 제외됨
라벨      → main · v2 · 0:08–0:16 · 되돌림
```

영상 교체도 확인했다 — HEAD 를 v1(180프레임)↔v2(36프레임) 로 옮길 때 작업의
`source_sha256` 과 `poseFrames` 가 함께 따라왔다. 되돌린 뒤 새 버전을 반영하면 지정이
풀리고 v5 가 현재가 된다.

## main 브랜치와 버전 이력 (2026-08-19)

가장 큰 구멍이던 출처 사슬을 채웠다. GitHub 의 main 브랜치를 그대로 옮겼다.

```
원작 게시            → v1 자동 생성 (state=merged, 구간=전체)
참여자가 구간 제안    → state=proposed
원작자/직접수정이 반영 → state=merged, main 의 다음 번호, 직전 head 를 부모로
거절                 → state=declined (지우지 않는다)
```

### 구간을 숫자로 구조화했다

`counts` 자유 텍스트가 아니라 **`count_from` / `count_to` 정수**다. 기록의 표준이 목표이므로
구간끼리 비교·검색할 수 있어야 한다. 비워 두면 전체 구간으로 기록한다.

### 스키마

```sql
versions(id, project_id, number, parent_id, title, note, author_id,
         count_from, count_to, source_sha256, video_url, width, height, frame_count,
         state, created_at, decided_at, decided_by)
```

`number` 는 반영 전에는 NULL 이다 — main 위 순번은 반영 시점에 부여된다.
`_sync_project_head()` 가 작업 카드의 `version` 라벨을 항상 `main · v2 · count 09–16` 처럼
head 로 맞추고, 영상이 붙은 버전이 반영되면 작업의 현재 영상도 그 버전 것으로 바꾼다.

### API

```
GET  /v1/projects/{id}/versions?user_id=            main · proposed · declined + canPropose/canDecide
POST /v1/projects/{id}/versions                     제안 (직접 수정 권한이면 바로 merged)
POST /v1/projects/{id}/versions/{vid}/decide        {accept: true|false}
```

권한 매핑: `보기만` 은 제안조차 403. `수정 제안` 은 제안만. `직접 수정`·원작자는 제안이 곧
반영이고 남의 제안을 처리할 수 있다. 이미 처리된 제안을 다시 처리하면 409.

### 화면

- **버전 화면** — 열린 제안(노란 카드, 반영/거절) → main 그래프(레일 + 점, HEAD 배지, v번호,
  구간 태그, 작성자·반영자) → 거절된 제안. 하드코딩된 `ver.3 · Original` 두 줄을 지웠다
- **제안 시트** — 제목 · 구간(시작–끝 카운트) · 메모. 권한에 따라 문구가 '제안 보내기' 또는
  'main 에 올리기' 로 바뀐다
- **작업 기록(Passport)** — 하드코딩 카드를 버리고 실제 버전 사슬에서 만든다. 반영된 버전 수,
  기여한 사람 수, 열린 제안 수, 기록된 구간 수 + 구간별 기여 목록
- Fork 타일은 Alert 만 띄우던 것이라 그리드에서 뺐다 (아래 남은 작업 참고)

### 검증 (브라우저 2개)

| 시나리오 | 결과 |
| --- | --- |
| 원작 게시 → v1 자동 생성 · HEAD 배지 | 통과 |
| `보기만` 권한의 제안 시도 | 403 |
| 참여자가 `count 09–16` 제안 | proposed 로 생성, 대기 문구 표시 |
| 참여자가 자기 제안을 반영 시도 | 403 |
| 원작자가 반영 → v2 | 작성자=참여자, 반영자=원작자로 기록 |
| 같은 제안 두 번 반영 | 409 |
| 거절 | declined 로 남음 |
| 작업 카드 라벨 | `main · v2 · count 09–16` |
| 부모 사슬 | v1 ← v2 연결 확인 |
| 작업 기록 화면 | 실제 버전·구간 반영 |

### 제안에 구간 영상 첨부 (2026-08-19)

제안이 메모만 남기던 것을 실제 영상 기록으로 만들었다. 제안 시트에서 그 구간을 촬영한 영상을
올리면 원작과 **똑같은 분석 파이프라인**을 타고, 결과가 그 버전에 붙는다.

`uploadForAnalysis(asset, onStage, onStarted, onDone)` 로 업로드·폴링을 한 곳에 모았다.
원작 게시(`processMotion`)와 제안 첨부(`attachProposalVideo`)가 같은 함수를 쓴다 — 구현이
둘로 갈라지면 한쪽만 고쳐지는 일이 생긴다. 상태는 호출한 쪽이 각자 들고 있어(`job` vs
`proposeJob`) 두 흐름이 서로를 건드리지 않는다.

- 시트 안에 진행률 막대와 단계 문구가 뜬다
- 분석 중에 보내기를 누르면 **예약**되고 끝나는 즉시 자동 전송된다 (원작 게시와 같은 방식)
- 버전·제안 카드에 `영상 180f` 태그로 첨부 여부와 검출 프레임 수를 표시한다
- 첨부는 선택이다 — 영상 없이 구간 + 메모만으로도 제안할 수 있다

**반영하면 작업의 현재 영상이 그 버전 것으로 바뀐다** (`_sync_project_head`). 확인한 결과
1080×1920 원작이 반영 후 320×240 제안 영상으로 교체되어 '영상 · 오버레이' 화면에 그대로
재생됐다.

검증:

| 시나리오 | 결과 |
| --- | --- |
| 제안에 영상 첨부 → 진행률 표시 | 통과 |
| 분석 중 보내기 → 예약, 다시 누르면 취소 | 통과 |
| 분석 완료 → 자동 전송, 시트 닫힘 | 통과 |
| 첨부 태그 `영상 180f` | 통과 |
| 반영 후 작업 영상이 제안 영상으로 교체 | 통과 (해상도로 확인) |
| 버전 사슬 v1(전체) → v2(구간 17–24, 영상 첨부) | 통과 |

### 남은 것

- **Fork** — 다른 작업으로 갈라져 나가는 것(`projects.parent_id`)은 아직 없다. main 안의
  버전 이력만 구현했다
- 구간 겹침 경고, 버전 간 비교(diff)
- 제안 첨부 영상은 원작과 같은 `uploads/` 를 쓴다. 같은 파일을 올리면 해시가 같아 재사용되는데,
  버전마다 별개 영상으로 취급하려면 버전 id 를 키에 넣어야 한다

## 포즈 데이터 화면 → 기록 파일 브라우저 (2026-08-19)

예전 '포즈 데이터' 화면은 프레임 하나의 33개 관절을 행으로 늘어놓고 `‹ ›` 로 프레임을 넘기는
형태였다. 1800 프레임 중 하나를 보려고 33행을 스크롤하는 화면이라 실용성이 없었다.

GitHub 저장소의 파일 목록처럼 바꿨다 — **무엇이 기록됐는지 보고, 열어 보고, 내려받는다.**
목표가 "기록의 표준"이라면 기록이 앱 밖으로 나갈 수 있어야 한다.

```
GET /v1/projects/{id}/files                     기록 파일 목록 (종류·설명·크기)
GET /v1/projects/{id}/files/{key}/preview       텍스트 앞 500줄 (줄 배열)
GET /v1/projects/{id}/files/{key}/download      Content-Disposition: attachment
```

- 화면 상단에 기록 형식 배지 `choreohub-motion-3d/v1`
- 파일 8종 + 원본 영상. json/csv 는 **보기**, 전부 **내려받기**
- 뷰어는 줄 번호가 붙은 blob 보기. `495,982줄 중 500줄 · 3.3 MB` 처럼 규모를 밝히고 잘림을 알린다
- 오버레이에서 넘어온 '지금 보던 프레임'은 JSON 조각으로 보여 준다 (관절 2개 + 안내)

### 다운로드가 왜 별도 엔드포인트인가

브라우저의 `<a download>` 는 **cross-origin URL 에서 무시된다.** 앱은 8081, 파일은 8000 이라
정적 `/uploads` 경로를 그대로 링크하면 다운로드가 아니라 **페이지 이동**이 된다 (실제로 그렇게
동작해서 뷰어가 사라졌다). 그래서 `Content-Disposition: attachment` 를 붙이는 엔드포인트를
따로 뒀다.

내보내는 파일 이름도 해시 대신 작업 이름을 쓴다 — `제목_없는_안무.motion_3d.csv`.

### 기록에서 절대 경로를 뺐다

`motion_3d.json` 의 `source` 가 서버의 절대 경로(`/Users/moon/Documents/...`)였다. 기록은
밖으로 나가는 것이 목적이므로 파일 이름만 남긴다. `uploads/` 에 있던 기존 산출물 3개도
같은 형태로 정리했다.

## SNS 커뮤니티 (2026-08-18)

다른 사람의 프로필을 보고 팔로우할 수 있다. 하단 탭 **탐색**이 실제 화면이 됐다
(예전에는 홈으로 가는 껍데기였다).

```
GET    /v1/community?viewer_id=            사람 목록 + 공개 작업 피드
GET    /v1/users/{id}/profile?viewer_id=   남의 프로필도 조회 (viewer 기준으로 계산)
POST   /v1/users/{id}/follow               팔로우
DELETE /v1/users/{id}/follow?viewer_id=    언팔로우
```

- `follows(follower_id, following_id)` 테이블. 자기 자신은 팔로우되지 않는다
- 프로필에 **팔로워 / 팔로잉 수와 팔로우 버튼**이 붙는다. 내 프로필에는 버튼이 없다
- 남의 프로필에서는 **설정·로그아웃 섹션이 숨는다**
- 프로필로 들어가는 경로: 탐색 목록, 공개 피드의 작성자, 협업 화면의 참여자,
  버전 화면의 원작자. 계정 없이 이름만 적어 초대한 사람은 열리지 않는다 (`user_id` 가 없다)

### 공유 범위를 커뮤니티에서도 지킨다

**`연습 전용` 작업은 남에게 보이지 않는다** (`store.PRIVATE_LICENSE`). 탐색 피드와 남의
프로필의 '고정한 작업'에서 빠지고, 본인에게만 보인다. 앱이 내세우는 공유 범위가 화면에서
실제로 의미를 갖게 하려는 것이다.

### 함께 고친 것

- 하단 탭의 '프로필'이 남의 프로필을 보던 상태를 물고 있었다. `navTo()` 가 '프로필' 탭으로
  갈 때 `viewingUserId` 를 푼다.
- 서버 데이터가 초기화되면 기기에 남은 `user_id` 는 더 이상 없는 사용자다. 예전에는 계속
  401 만 떴는데, 이제 신원을 지우고 이름 입력 화면으로 돌아간다.

### DB 초기화

데모 전에 테스트 계정을 치우려면:

```bash
cd mediapipe-service
rm -f choreohub.db choreohub.db-wal choreohub.db-shm
# 서버 재시작 — 첫 사용자가 다시 예시 작업을 받는다
```

브라우저에 남은 신원은 위 401 처리 덕분에 자동으로 정리된다.

## 프로필을 GitHub 형태로 (2026-08-18)

프로필도 하드코딩이었다 — "지성 문", 12/36/184. 이제 `GET /v1/users/{id}/profile` 이 실제
활동을 요약해 준다.

GitHub 프로필의 뼈대를 그대로 가져왔다.

| GitHub | ChoreoHub |
| --- | --- |
| 아바타 · 이름 · @handle · 가입일 | 같음 (`handle` 은 `user_id` 앞 7자) |
| repositories / followers / following | 원작 작업 / 참여 중 / 함께한 사람 / 포즈 프레임 |
| contribution graph | **기여 달력** — 53주 × 7일, 5단계 색 |
| pinned repositories | 고정한 작업 (내 작업 4 + 참여 작업 2) |
| activity feed | 최근 활동 10건 |

**기여로 세는 것**: 내가 만든 작업, 내 작업에 누군가를 초대한 일, 내가 초대받아 참여한 일.
모두 행의 `created_at` 을 쓴다. 색은 GitHub 의 초록 대신 브랜드 파랑 5단계다.

달력은 가로 스크롤이고 **오늘이 오른쪽 끝**이라, 열릴 때 `scrollToEnd` 로 최신 주부터 보여
준다. 이게 없으면 1년 전 빈 칸만 보인다. 아직 오지 않은 날짜는 `c: -1` 로 내려보내 투명하게
그린다.

`graphRef` 는 App 최상위에 둔다 — `Profile` 은 컴포넌트가 아니라 함수로 호출되므로 그 안에서
훅을 쓸 수 없다 (위 '페이지를 요소가 아니라 함수 호출로' 참고).

로그아웃 버튼도 여기에 붙였다 (`signOut`).

## 공동 작업자 UI (2026-08-18)

협업 화면은 가짜 데이터였다 — 기여자 3명과 "전체 기여자 8"이 하드코딩되어 있었다. 이제 실제로
동작한다.

- `Collaborator = { id, name, role, counts, permission }` 을 `Project.collaborators` 에 붙인다
- 권한은 창작자 언어로 세 단계: **보기만 / 수정 제안 / 직접 수정**
- 초대 → 수정 → 참여 해제 전부 동작하고, 화면을 벗어났다 돌아와도 남는다
- `Project.inviteCode` 로 `choreohub.app/join/<코드>` 링크를 보여 준다. 계정·인증이 없으므로
  실제로 가입시키는 링크가 아니라 공유 화면용이다 — 나중에 인증을 붙일 때 이 코드를 그대로 쓸 수 있다
- Version 화면의 기여자 수도 실제 값을 읽는다

`updateSelected()` 로 `selected` 와 `createdProjects` 안의 같은 객체를 함께 갱신한다. 한쪽만
바꾸면 화면을 다시 열었을 때 초대한 사람이 사라진다.

이 절은 UI 를 만든 시점의 기록이다. 지금은 위 '실제 다중 사용자 협업' 대로 서버에 저장되고,
초대·수정·해제가 모두 API 를 거친다.

### 함께 고친 것

- `CreateModal` 이 홈 화면에만 렌더링되어 있어서 **다른 화면의 `＋` 버튼이 아무 반응도 하지
  않았다.** 모달들을 App 루트로 올렸다 (`{CreateModal()}{CollabSheet()}`).
- 바텀 시트에 높이 상한과 스크롤이 없어 입력 항목이 많으면 화면 밖으로 잘렸다.

5. **유사 안무 탐색 도구는 후순위**
   - 향후 확장 가능: 등록된 8카운트 구간과 새 영상의 관절·타이밍을 비교해 출처 후보를 제안
   - 그러나 “표절 판정”이 아니라 `허용된 리믹스 / 승인 필요 / 출처 미표기 / 검증 불가` 같은 안내 도구로 설계해야 한다.

## 기술 주의사항

- Expo SDK 57 기반이다. Expo 관련 수정 전에는 해당 버전 공식 문서를 확인한다.
- 오버레이 드로잉에 `react-native-svg`(15.15.4)를 쓴다. Expo Go 에 기본 포함된 패키지라
  네이티브 재빌드가 필요 없고 웹에서도 그대로 동작한다.
- 이 머신에서 8000(mediapipe-service)·8081(Expo)은 상시 점유 중이다. 검증용으로 다른
  인스턴스를 띄울 땐 빈 포트를 쓰고, 앱에는 `EXPO_PUBLIC_MEDIAPIPE_API_URL` 로 넘긴다.
- 수정 전 원본은 `.backup/` 에 있다 (`App.tsx.bak`, `main.py.bak`). 저장소에 아직 커밋이
  하나도 없어서 되돌릴 지점이 없다 — 초기 커밋을 만들어 두는 편이 안전하다.
- 물리 모바일 기기에서 `localhost:8000`은 휴대폰 자신을 의미한다. 개발 중에는 컴퓨터의 LAN IP 또는 배포된 HTTPS 서버 URL을 환경 변수에 넣어야 한다.
- `App.tsx`가 하나의 큰 파일이라 기능 추가 전 컴포넌트/상태 분리를 고려해도 좋다. 다만 데모 안정성이 우선이다.
- 현재 `npx tsc --noEmit` 통과를 확인했다.
- 서버의 파일 산출물은 현재 로컬에 그대로 공개되는 `/uploads` 정적 경로다. 운영 전에는 인증·접근 권한·보관 정책을 설계해야 한다.

## Claude에게 바로 전달할 요청문

```
이 프로젝트는 ChoreoHub라는 “안무가를 위한 GitHub” 앱입니다. 아래 인수인계 문서를 먼저 읽고, 현재 구현을 훼손하지 않는 범위에서 이어서 작업해 주세요.

파일: /Users/moon/Documents/멋사 중앙해커톤/choreohub-mobile/CLAUDE_HANDOFF.md

가장 먼저 현재 앱과 MediaPipe 서버를 실행해 실제 업로드 → 분석 → 오버레이 영상 재생 흐름을 확인하세요. 그 뒤, 우선순위는 (1) 3D 스켈레톤 결과와 JSON/CSV/NPY 산출물을 앱에서 명확히 보여주기, (2) 업로드 진행 및 실패 UX 개선, (3) 데이터 영속화 설계입니다.

제품의 핵심은 안무 업로드·공유·버전·Fork·공동작업이며, 유사도 점수나 법률 판정 서비스로 바꾸지 마세요. 앱 문구도 법률 용어 대신 창작 기록과 협업 중심으로 유지해 주세요.
```
